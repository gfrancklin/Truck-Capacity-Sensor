/*!
 * debug.c
 *
 *  Created on: Feb 20, 2018
 *      Author: Gabriel
 */

#define MEAS_BUF_SIZE	256

void print_measurement(int measurement) {
	char ESC = 27;	//ESC character
	char Buf[MEAS_BUF_SIZE];
	memset(Buf, 0, MEAS_BUF_SIZE);
	snprintf(Buf, );
	CDC_Transmit_FS((uint8_t *)Buf, 13);
}

